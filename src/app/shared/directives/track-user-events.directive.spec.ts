import { TrackUserEventsDirective } from './track-user-events.directive';

describe('TrackUserEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackUserEventsDirective();
    expect(directive).toBeTruthy();
  });
});
